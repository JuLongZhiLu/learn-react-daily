## 第 12 天：Context（上下文）——干掉繁琐的逐层传参

在前 11 天的学习中，我们一直在用 **Props** 传递数据。在组件层级比较浅的时候，Props 非常直观好用。

但是，随着项目规模的扩大，你会遇到一个在 React 中非常经典的痛点：**Props 钻孔（Props Drilling）**。

今天，我们来学习 React 官方提供的一辆“数据地下铁”：**Context（上下文）**，彻底干掉繁琐的逐层传参。

---

## 痛点：烦人的“Props 钻孔”

想象一下，你正在开发一个包含“夜间模式”的主题切换功能：

1. 最顶层的 `App` 组件记录了当前的主题：`const [theme, setTheme] = useState('light')`。
2. 页面中间有一个导航栏组件 `Navbar`。
3. 导航栏内部有一个用户头像组件 `UserAvatar`。
4. 头像组件内部有一个设置按钮组件 `SettingButton`。

如果 `SettingButton` 需要知道当前是 `'light'` 还是 `'dark'`，并且需要修改这个状态：

```text
 ┌──────────────┐
 │     App      │ ── (theme, setTheme)
 └──────────────┘
        │   
 ┌──────────────┐
 │    Navbar    │ ── (只是做快递员，自己并不用 theme)
 └──────────────┘
        │   
 ┌──────────────┐
 │  UserAvatar  │ ── (只是做快递员，自己也用不上 theme)
 └──────────────┘
        │   
 ┌──────────────┐
 │SettingButton │ ── 终于拿到了数据！
 └──────────────┘
```

为了把数据传给 `SettingButton`，`Navbar` 和 `UserAvatar` 必须充当“快递员”，强行在自己的 Props 里接收、传递这两个参数。

这不仅导致中间组件的代码变得非常冗余，而且极难维护。

---

## Context 的魔法：建立你的“广播电台”

**Context** 的设计理念非常巧妙，它摒弃了“手递手传快递”的模式，转而建立了一个**“广播电台”**：

* **发射台（Provider）**：顶层组件设立一个广播发射台，把数据（比如主题、用户信息）广播出去。
* **接收器（useContext）**：不管底层的组件埋得多深，只要它想用这些数据，只需要在内部安装一个“接收器”，就能直接拉取广播数据。
* **中间组件（Navbar / UserAvatar）**：完全不需要做任何事，数据会直接“穿透”它们。

---

## 使用 Context 的三步套路

在代码中，使用 Context 只需要极富逻辑的三步：

### 第一步：创建广播台（`createContext`）
在单独的地方或文件里，创建一个 Context 对象：
```jsx
import React, { createContext } from 'react';

// 创建并导出一个主题上下文
export const ThemeContext = createContext();
```

### 第二步：发射信号（`ThemeContext.Provider`）
在父组件（如 `App`）中，使用 `Provider` 组件把子组件包起来，并通过 `value` 属性把你想广播的数据发出去：
```jsx
function App() {
  const [theme, setTheme] = useState('light');

  return (
    // 用 Provider 包裹，value 里的内容会被广播给所有子孙组件
    <ThemeContext.Provider value={{ theme, setTheme }}>
      <Navbar /> 
    </ThemeContext.Provider>
  );
}
```

### 第三步：接收信号（`useContext`）
在深层子组件中，引入刚刚创建的 `ThemeContext`，并用 `useContext` Hook 接收数据：
```jsx
import React, { useContext } from 'react';
import { ThemeContext } from './App'; // 引入上下文

function SettingButton() {
  // 直接拿到广播里的 theme 和 setTheme，不需要经过任何父组件传递！
  const { theme, setTheme } = useContext(ThemeContext);

  return <button onClick={() => setTheme('dark')}>当前主题：{theme}</button>;
}
```

---

## 动手练习

我们回到 React Playground 网站：

[React Playground](https://playcode.io/react?utm_source=chatgpt.com)

把下面的代码复制到 `index.jsx`，来亲自体验数据是如何“穿透”中间组件的：

```jsx
import React, { useState, createContext, useContext } from 'react';
import { createRoot } from 'react-dom/client';

// 🛠️ 1. 创建上下文（广播台）
const ThemeContext = createContext();

// 2. 中间组件 1 (Toolbar) —— 注意：它接收 0 个 Props！
function Toolbar() {
  console.log('🔄 Toolbar 渲染了');
  return (
    <div style={{ padding: '12px', border: '1px solid #e2e8f0', borderRadius: '8px', marginTop: '12px' }}>
      <h4>栏目导航（中间组件 Toolbar）</h4>
      <p style={{ fontSize: '13px', color: '#64748b' }}>我只是中间人，我没有接收任何关于主题的 Props。</p>
      
      {/* 嵌套下一级组件 */}
      <ThemeButton />
    </div>
  );
}

// 3. 深层子组件 (ThemeButton) —— 直接跨越 Toolbar 接收 App 的广播
function ThemeButton() {
  // 💡 使用 useContext 直接拿到发射台的数据！
  const { theme, setTheme } = useContext(ThemeContext);

  const isDark = theme === 'dark';

  return (
    <div style={{ padding: '12px', backgroundColor: isDark ? '#334155' : '#f8fafc', borderRadius: '6px', marginTop: '12px' }}>
      <p style={{ margin: '0 0 10px 0', color: isDark ? '#f8fafc' : '#0f172a' }}>
        我是底层的按钮组件。当前主题：<b>{theme}</b>
      </p>
      
      <button 
        style={{
          backgroundColor: isDark ? '#f1f5f9' : '#0f172a',
          color: isDark ? '#0f172a' : '#f1f5f9',
          border: 'none',
          padding: '8px 12px',
          borderRadius: '4px',
          cursor: 'pointer',
          fontWeight: 'bold'
        }}
        onClick={() => setTheme(isDark ? 'light' : 'dark')}
      >
        切换为 {isDark ? '💡 明亮模式' : '🌙 暗黑模式'}
      </button>
    </div>
  );
}

// 4. 最顶层组件 (App)
function App() {
  const [theme, setTheme] = useState('light');

  const isDark = theme === 'dark';

  const containerStyle = {
    padding: '24px',
    maxWidth: '400px',
    margin: '30px auto',
    borderRadius: '16px',
    fontFamily: 'sans-serif',
    border: '1px solid #cbd5e1',
    boxShadow: '0 10px 15px -3px rgba(0,0,0,0.1)',
    transition: 'all 0.3s ease',
    backgroundColor: isDark ? '#1e293b' : '#ffffff',
    color: isDark ? '#f8fafc' : '#1e293b'
  };

  return (
    // 💡 2. 用 Provider 包裹，发射 theme 和 setTheme 信号
    <ThemeContext.Provider value={{ theme, setTheme }}>
      <div style={containerStyle}>
        <h2>🚇 Context 穿透演示</h2>
        <p>顶层的 App 记录了当前的主题状态。</p>

        {/* 渲染中间组件，不向它传递任何关于主题的 Props */}
        <Toolbar />
      </div>
    </ThemeContext.Provider>
  );
}

const root = createRoot(document.getElementById('root'));
root.render(<App />);
```

尝试在右侧 Preview 窗口中：
1. 点击“切换主题”按钮，观察卡片整体颜色（App）和最深层按钮的卡片（ThemeButton）如何同时切换主题色。
2. 仔细检查 `Toolbar` 组件的代码：**它没有向 `ThemeButton` 传递任何 Props**。
3. 数据就像坐上了地下铁，直接从 `App` 发射台，无声地穿透了中间的 `Toolbar`，送到了最底层的 `ThemeButton` 手中。

这就是 React 中处理全局共享状态（如主题、语言、用户信息）的终极方案。

---

## 明天可以学什么？

恭喜你！到今天为止，你已经把 React 最重要、最常用的一整套核心理念全部攻克了！

但在真实的开发中，我们不可能把所有的代码都塞在单单一个文件里。当你的项目包含几十个组件时，我们需要使用前端工程化的手段：
* 怎么划分项目文件夹（`components`、`hooks`、`context` 怎么放）？
* 一个合格的 React 生产级工程项目到底长什么样？

明天我们来学一个极其接地气、对你在实际开发或找工作中非常有帮助的主题：**第 13 天：React 生产级项目结构与最佳实践**
